# Core
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
sqlalchemy>=2.0,<3
python-multipart>=0.0.9,<1   # multipart image uploads

# OCR / CV
# Requires Python <=3.12: paddlepaddle publishes no wheel for 3.13/3.14.
# paddleocr must be 3.x - ocr_service.py uses the 3.x API (predict(),
# rec_texts/rec_scores/rec_polys). The old <3 pin silently installed 2.10,
# which has none of those and fails at the first inspection.
# paddlepaddle is pinned to 3.3.x: 3.0.0 is incompatible with paddleocr 3.7
# ("Type of attribute: strides is not right").
paddlepaddle>=3.3,<3.4
paddleocr>=3,<4
opencv-python>=4.9,<6
numpy>=1.26,<3
pillow>=10,<13               # visual evidence overlays

# Reports
reportlab>=4,<6              # PDF inspection reports

# Frontend
gradio>=5,<7

# Tests
pytest>=8,<9
httpx>=0.27                  # required by fastapi.testclient

# Optional: grounded answer generation for /rag/ask. Without groq the RAG
# endpoint still answers, extractively, by quoting retrieved clauses verbatim.
# python-dotenv loads the local .env; plain environment variables work without it.
groq>=1,<2
python-dotenv>=1,<2
